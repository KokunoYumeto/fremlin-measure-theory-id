# CP0004 — O007-FREMLIN-V1-S114 Admission

Date: 2026-08-22 (Europe/Berlin)

## Boundary

Complete Section 114, `Lebesgue measure on $\Bbb R$` → `Ukuran Lebesgue
pada $\Bbb R$`, not an excerpt. Authority: 25,717 bytes / 612 lines /
SHA-256
`206488ff5ba2960f4e130d162cca6df7af2935968754d77bc18b53ab084b8f97`.
Target: 28,148 bytes / 650 lines / SHA-256
`3d29f5c0dea66737852e085632cbf51d77c1bb391fe59916b39c5c9ab9db2030`.

The source-native 2011 Volume 1 replay is 102 pages and places Section 114
on printed pages 23–28. Page 23 is shared with Section 113 and page 28 with
Section 115. The cumulative admitted union through this boundary is therefore
the 19 unique official pages 10–28, never the naïve sum of section ranges.

## Fixed admission evidence

- Structural and mathematical replay: all 438 mathematical atoms, 1,167
  active commands, 1,157 symbolic commands, 28 explicit source IDs, 71
  protected references, nine reader-text atoms, and eight source hints are
  preserved with no symbolic delta or active English residue.
- Semantic review: every definition, result, proof qualifier, endnote, all 19
  exercises (`114Xa`–`114Xg`, `114Ya`–`114Yl`), both hints inside `114Yd`,
  and all other hints were read against the authority. All 43 printed
  cross-reference expressions expand to the expected 51 typed edges. Exact
  evidence is in `qa/mt114-semantic-review.json`.
- Translation review corrected two optional `\cmmnt` placement shifts before
  admission and refined three stiff Indonesian phrases. No source correction
  was asserted; no high-confidence source defect was found.
- Backend: 686 unit-local records — 45 segments, six definitions, five
  results, 17 proofs, 19 exercises, eight hints, 75 relations, 54
  cross-references (51 printed edges plus three curricular-route edges), 16
  terms, 438 formulas, two artifacts, and one QA event. There are no S114
  source assets. Unit manifest SHA-256:
  `94af0c5ec39954d1ce44e4f9ecf7cdf6d533f0893d079de0590f415dad15c15b`.
- The cumulative catalog has one corpus, two volumes, four admitted units, one
  rights record, and 18 resources. Its manifest SHA-256 is
  `4e28100bca6d1ff68905e6084cbf60a1d752ac974b3c164dd2ab6c09e733adc5`.
  Schema v1.1 and every prior S111–S113 unit record/manifest remain exact.
- Rights: Fremlin-derived source and reader material remains under the Design
  Science License with editable source, attribution, modification notice, and
  no added restriction. The four S113 diagrams and their exact provenance are
  retained; S114 introduces no component asset.

## Final reader and visual predicate

This checkpoint is fail-closed. `O007-FREMLIN-V1-S114` becomes admitted if and
only if all of the following exact live receipts exist and pass:

1. `qa/mt114-build-receipt.json` binds the four authority members and target
   files, a reproducible two-pass cumulative build, a 23-page `id-ID` PDF,
   deterministic HTML/package/ZIP outputs, and byte-exact preservation of all
   S111–S113 release artifacts.
2. `qa/mt114-reader-qa.json` has `pass: true` and validates the four-unit
   semantic HTML reader, 1,713 visible HTML formula sources (445 + 480 + 352
   + 436), 61 cumulative exercises, 14 cumulative hints, local MathJax,
   localized generated labels, embedded PDF fonts, exact figure use, backend
   closure, package manifests, checksums, and ZIP metadata.
3. `qa/mt114-visual-browser-qa.json` has `pass: true`, binds the same final
   PDF/HTML bytes, and records all-page PDF inspection plus desktop/mobile
   browser replay with centered readable pages, no clipping or page-level
   overflow, container-local long-math overflow, completed MathJax, working
   anchors/links, and accessible S113 diagrams.

Before all three predicates are true, this unit remains pending and must not
be published. Once they are true, this record admits the exact target hash
above without a further rewrite, and the active source-order cursor advances
to `O007-FREMLIN-V1-S115`. Dynamic PDF, ZIP, package, and visual identities
remain in their canonical receipts rather than being duplicated into this
package input.

## Publication boundary

After the predicate passes, the authorized immutable public target is the
existing corpus repository and prerelease tag `v0.4.0-s114`. The release must
contain exactly the cumulative PDF, deterministic ZIP, and checksum file;
every asset and every boundary file must pass anonymous byte-for-byte
readback. The sanitized public IDs, URLs, commit/tree identities, and hashes
belong in `qa/PUBLICATION_RECEIPT_S114.json` and the post-release current-state
record. No upstream issue or message is part of this boundary.
