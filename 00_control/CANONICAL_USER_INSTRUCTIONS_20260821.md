# Private canonical control omitted from the public package

This same-path public placeholder intentionally withholds the private operating-control text. The canonical local record is unchanged; only its byte count and SHA-256 are retained in PUBLIC_SANITIZATION_MAP.json.
